<?php
$title = 'Doações';
$this->headTitle($title);
?>
<div class="card" style="margin-top: 10px;">
    <div class="card-header" style="background: #303641;color:white;">
        <h1><?= $this->escapeHtml($title); ?></h1>
        <p>
            <a class="btn btn-flat btn-success mb-3" style="margin-top: 8px;" href="<?= $this->url('doacao', ['action' => 'add']) ?>">Nova Doação</a>
        </p>
    </div>
    <div class="card-body">
        <table class="table">
            <tr>
                <th>Titulo</th>
                <th>Descrição</th>
                <th>&nbsp;</th>
            </tr>
            <?php foreach ($this->paginator as $doacao) : // <-- change here! ?>
                <tr>
                    <td><?= $this->escapeHtml($doacao->titulo ) ?></td>
                    <td><?= $this->escapeHtml($doacao->descricao) ?></td>
                    <td>
                        <a href="<?= $this->url('doacao', ['action' => 'edit', 'id' => $doacao->id]) ?>">Edit</a>
                        <a href="<?= $this->url('doacao', ['action' => 'delete', 'id' => $doacao->id]) ?>">Delete</a>
                    </td>
                </tr>
            <?php endforeach; ?>
        </table>
    </div>
</div>

<?=
$this->paginationControl(
// The paginator object:
        $this->paginator,
        // The scrolling style:
        'sliding',
        // The partial to use to render the control:
        'partial/paginator',
        // The route to link to when a user clicks a control link:
        ['route' => 'doacao']
);
?>