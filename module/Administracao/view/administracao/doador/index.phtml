<?php
$title = 'Doadores';
$this->headTitle($title);
?>
<div class="card" style="margin-top: 10px;">
    <div class="card-header" style="background: #303641;color:white;">
        <h1><?= $this->escapeHtml($title); ?></h1>
        <p>
            <a class="btn btn-flat btn-success mb-3" href="<?= $this->url('doador', ['action' => 'add']) ?>">Novo Doador</a>
        </p>
    </div>
    <div class="card-body">
        <table class="table">
            <tr>
                <th>Nome</th>
                <th>Email</th>
                <th>&nbsp;</th>
            </tr>
            <?php foreach ($this->paginator as $doador) : // <-- change here! ?>
                <tr>
                    <td><?= $this->escapeHtml($doador->nome ) ?></td>
                    <td><?= $this->escapeHtml($doador->email) ?></td>
                    <td>
                        <a href="<?= $this->url('doador', ['action' => 'edit', 'id' => $doador->id]) ?>">Edit</a>
                        <a href="<?= $this->url('doador', ['action' => 'delete', 'id' => $doador->id]) ?>">Delete</a>
                    </td>
                </tr>
            <?php endforeach; ?>
        </table>
    </div>
</div>

<?=
$this->paginationControl(
// The paginator object:
        $this->paginator,
        // The scrolling style:
        'sliding',
        // The partial to use to render the control:
        'partial/paginator',
        // The route to link to when a user clicks a control link:
        ['route' => 'doador']
);
?>
